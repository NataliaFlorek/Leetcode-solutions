# Write your MySQL query statement below
SELECT query_name, ROUND(SUM(rating/position)/COUNT(position),2) AS quality, ROUND(SUM(rating<3)/count(position),4)*100 AS poor_query_percentage FROM Queries GROUP BY query_name;